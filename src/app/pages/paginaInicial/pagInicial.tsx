import React from 'react';
import { View, Text, StyleSheet, ImageBackground, Image } from 'react-native';

export default function PagInicial(){
    return (
        <ImageBackground style={{flex:1}} source={require("@/assets/images/pag_inicial.jpg")}>
            
        </ImageBackground>
      )
    }
    
